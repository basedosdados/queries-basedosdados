name: Change metadata status to "production"

on:
  workflow_run:
    workflows: ["Table approve"]
    types:
      - completed
    branches:
      - master

jobs:
  change-metadata-status:
    name: Change metadata status to "production"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Get all changed files using a comma separator
        id: changed-files
        uses: tj-actions/changed-files@v35
        with:
          separator: ','

      - name: Setup Python 3.9
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-actions.txt

      - name: Run script for changing metadata status
        run: |
          echo "TODO: Add script for changing metadata status"